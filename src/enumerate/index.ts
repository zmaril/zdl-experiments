/**
 * Enumeration of positions / entanglement classes (phase 2).
 *
 * Enumerates candidate positions (partition x hammerlock x entanglement data)
 * and classifies them with invariants from src/core, reconciling with the
 * 157 feasible states of the partition-only model.
 */
export {};
