{
  "name": "@zdl/hands-regions",
  "version": "0.1.0",
  "private": true,
  "description": "Contact-region combinatorics of a single grip between two hands: enumerate raw contact-pair sets, filter by anatomical feasibility rules, categorize survivors, and sanity-check against named dance grips.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "pretest": "tsc --noEmit",
    "test": "node --import tsx --test test/model.test.ts test/named-grips.test.ts test/enumeration.test.ts",
    "report": "tsx src/report.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
