{
  "name": "hands-kinematics",
  "version": "0.1.0",
  "private": true,
  "description": "Functional classification of two-dancer grips as kinematic pairs: per-axis relative-motion states, force-transmission channels, and closure regimes; enumerate the abstract grip space, filter to mechanical consistency, and derive a grips-by-moves compatibility matrix.",
  "type": "module",
  "scripts": {
    "test": "tsx src/test.ts",
    "report": "tsx src/report.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
