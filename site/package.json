{
  "name": "zdl-experiments-site",
  "version": "0.1.0",
  "description": "Static site publishing the dance-connection research notes in this repo. Globs the repo for */NOTES.md at build time and renders each as an experiment page.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.14.1",
    "katex": "^0.16.22",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
